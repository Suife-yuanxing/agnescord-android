# 问题跟踪清单 — 更新于 2026-06-24（第 9 轮后）

> 测试设备：OPPO PKG110（Android 16 / SDK 36，ColorOS）
> APK：`agnescord-android/android/app/build/outputs/apk/debug/app-debug.apk`
> 后端：`http://129.211.7.67:8766`（`PLATFORM_TESTING=true`，60+ 端点全部署）

---

## 一、聊天页

| # | 问题 | 状态 | 说明 |
|:--|------|:--:|------|
| A1 | 历史记录倒置 | ✅ | `d.messages.reverse()` |
| A2 | 上传图片 | ⚠️ | 后端端点已部署，`onShowFileChooser` 已加。**待验证**：选图片后是否弹出文件选择器 |
| A3 | 键盘输入框浮起 | ✅ | 第 9 轮修好：Java `ViewTreeObserver` 检测键盘 CSS px → JS `position:fixed; bottom:键盘高度` |
| A4 | 新建Bot残留演示消息 | ✅ | HTML 演示消息已删除，改为"加载中…" |
| A5 | Bot信息回填（人格） | ✅ | `getBot` → personality label |
| A6 | Bot头像回填 | ⚠️ | 代码已接 `getBot` → `avatar_url`。**待验证**：头像更换功能（点头像→文件选择器） |
| A7 | 语音 | 🔴 | **暂时屏蔽**（麦克风按钮隐藏）。录音桥接已写，但播放不稳定 |

---

## 二、API Key 管理

| # | 问题 | 状态 | 说明 |
|:--|------|:--:|------|
| B4 | 用量明细 | 🔴 | `loadUsageDetail` 函数存在，`apiReady()` 检查延后到点击时。**未验证是否加载出真实数据** |
| — | 添加 Key | 🔴 | `toggleNewForm`/`createKey` 存在。**同上，未验证** |

---

## 三、我的页

| # | 问题 | 状态 | 说明 |
|:--|------|:--:|------|
| C1 | 个人资料保存 | ⚠️ | 刚修复：gender 中文→英文映射（`男→male, 女→female`）。**待验证** |
| C2 | 生日选择器 | 🔴 | 三列 tap-to-select 组件已完成，`requestAnimationFrame` 滚动。**用户反馈日期仍有缺失** |
| C3 | 头像回显 | ✅ | `getProfile` → `avatar_url` |
| C4 | 字号切换 | ✅ | `syncFontSize` 改为 `DOMContentLoaded` + `querySelector` |
| C5 | 通知开关 | ✅ | `toggleToast` → `updateUserSettings` |
| C6 | 系统权限 | 🔴 | ColorOS 封锁弹窗。`NativeBridge.requestPermission` 已加 fallback（跳系统设置）。**需手动去系统设置开权限** |
| C7 | 返回位置恢复 | ✅ | `scroll` 事件保存 `scrollTop` |
| C9 | 协议邮箱 | ✅ | `2938897660@qq.com` |

---

## 四、Bot 管理

| # | 问题 | 状态 | 说明 |
|:--|------|:--:|------|
| D1 | 左滑管理 Bot | ✅ | 滑动 + 设置/删除层 |
| D2 | 创建向导返回按钮 | ✅ | `href` → `我的Bot.html` |
| D3 | 输入框展开 | ✅ | 改为随内容自动变高（`scrollHeight`），不再 focus 固定高度 |

---

## 五、综合

| # | 问题 | 状态 | 说明 |
|:--|------|:--:|------|
| E1 | 胶囊速度 | ✅ | 150+150ms |
| E2/E3 | 胶囊形状 | ✅ | width+8/left-4 |
| E4 | 点击方块 | ✅ | `-webkit-tap-highlight-color:transparent` |
| E5 | 首页活跃度 | ⚠️ | 已改为 `d.today.messages`。**未 CDP 验证** |
| E8 | 通知按钮残影 | ✅ | 涟漪排除跳转元素 |
| E10 | 长按浮出 | ✅ | `user-select:none` + `setOnLongClickListener` |
| — | 聊天背景切换 | 🔴 | dark-mode CSS 与 `setProperty important` 冲突 |
| — | 外观自定义 | 🔴 | 同上根因 |
| — | 聊天滚动到底 | 🔴 | flex 容器 `scrollTop=999999` 不生效 |
| — | 图片上传后持久化 | 🔴 | 已替换预览 URL 但未写消息历史 |

---

## 汇总

| 状态 | 数量 | 问题 |
|:--|:--:|------|
| ✅ 已修复验证 | **17** | A1/A3/A4/A5/C3/C4/C5/C7/C9/D1/D2/D3/E1/E2/E3/E4/E8/E10 |
| ⚠️ 刚修待验 | **4** | A2(上传图片)、A6(Bot头像)、C1(资料保存)、E5(活跃度) |
| 🔴 仍未解决 | **8** | A7(语音)、B4(用量)、Key添加、C2(生日)、C6(权限)、聊天背景、聊天滚动、图片持久化 |

---

*最后更新：2026-06-24 第 9 轮修复后*
*记录人：ZCode 真机调试会话*
