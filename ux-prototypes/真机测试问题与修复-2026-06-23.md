# 安卓真机测试问题与修复记录 — 2026-06-23

> 测试设备：OPPO PKG110（Android 16 / SDK 36，1080×2376，USB 调试连接）
> 测试 APK：`agnescord-android/android/app/build/outputs/apk/debug/app-debug.apk`
> 后端：`http://129.211.7.67:8766`（FastAPI，`PLATFORM_TESTING=true`）
> 测试方法：`adb` logcat 诊断 + WebView CDP 远程调试（`setWebContentsDebuggingEnabled(true)`）+ 本地 Ollama 视觉模型（llava:13b / moondream:1.8b）读截图

---

## 一、问题清单与状态

| # | 问题 | 根因 | 状态 |
|:--:|------|------|:----:|
| 1 | 手机浏览器打不开 `http://129.211.7.67:8766` | 手机浏览器/移动网络对非标准端口的限制；**App WebView 实际可达**（logcat 实测 `/api/v1/dashboard` 返回 401 = HTTP 响应已到达） | ✅ 非 bug，无需改代码 |
| 2 | Bot 无法创建、其他功能无真实数据 | ① 测试直登 mock token 被后端 401 拒（设计如此）；② **`config.js` 依赖 `native.js` 时序的隐藏 bug**——12 个页面未加载 `native.js`，导致 `APP_CONFIG=file://`，API 请求打到 `file:///api/v1/...` 全失败 | ✅ bug 已修；401 需注册真实账号 |
| 3 | 侧边返回直接退出应用 | Android 16 (SDK 36) 上旧 `onBackPressed()` 已废弃 + 手势双触发走到 `finishAndRemoveTask()` | ✅ 已修复 |
| 4 | 各页面顶部色块不同步、兼容差 | 各页面**硬编码不同 header 渐变**（首页粉/Bot创建向导珊瑚三色/聊天页 token 双色/登录页无背景），切换页面色块跳变 | ✅ 已修复并验证 |
| 5 | "APP 打不开"（白屏） | 启动页 `doNavigate()` 调 `API.isLoggedIn()` 时若 JS 异常无兜底 → 白屏无提示；`MainActivity.onReceivedError` 只对 `index.html` 兜底，`启动页.html` 失败时静默白屏 | ✅ 已修复 |

---

## 二、修复详情

### 修复 A：`config.js` 直接检测 `NativeBridge`（解决问题 2 的核心 bug）

**文件**：`shared/config.js`

**根因**：原 `config.js` 用 `if (window.NativeApp && window.NativeApp.isNative)` 判断是否在 APK 内。但 `window.NativeApp` 由 `native.js` 定义，而 **12 个页面未加载 `native.js`**（见下表），导致 `NativeApp` 不存在 → `config.js` 走 `else` 分支设 `server_base = location.origin` = `file://` → `api.js` 的 `_base()` 返回 `file://` → 所有 API 请求打到 `file:///api/v1/...` → `Fetch API cannot load file:///api/v1/bots. URL scheme "file" is not supported.`

**未加载 `native.js` 的 12 个页面**：`我的Bot` / `Bot设置` / `API Key管理` / `QQ通道` / `微信通道` / `修改密码` / `数据权限` / `数据面板` / `管理员面板` / `编辑个人资料` / `通知` / `黑名单`

**修复**：`config.js` 直接检测原生注入的 `window.NativeBridge`（`addJavascriptInterface` 注入，一定先于任何 `<script>` 存在），不再依赖 `native.js` 的执行时序：

```js
var _isNative = false;
try {
  if (window.NativeBridge && typeof window.NativeBridge.isNative === 'function') {
    _isNative = window.NativeBridge.isNative();
  }
} catch (e) { _isNative = false; }
if (_isNative || (window.NativeApp && window.NativeApp.isNative)) {
  window.APP_CONFIG.server_base = 'http://129.211.7.67:8766';
} else {
  window.APP_CONFIG.server_base = location.origin;
}
```

**验证**（logcat DIAG 实测）：
- 修复前 `我的Bot.html`：`APP_CONFIG=file://` + `Fetch API cannot load file:///api/v1/bots`
- 修复后 `我的Bot.html`：`APP_CONFIG=http://129.211.7.67:8766` + `HTTP error 401 — http://129.211.7.67:8766/api/v1/bots`（请求正确打到后端，仅 mock token 被拒）

> 注：`NativeApp=undefined` 在这些页面仍存在（因没加载 native.js），但**不影响功能**——`config.js` 现在直接检测 `NativeBridge`。后续可补齐 12 页的 `native.js` 引用以求彻底，但非阻塞。

---

### 修复 B：返回键改用 `OnBackPressedDispatcher`（解决问题 3）

**文件**：`agnescord-android/android/app/src/main/java/com/deepseekqq/agnescord/MainActivity.java`

**根因**：手机是 Android 16 (SDK 36)，旧 `onBackPressed()` 已废弃；且手势返回双触发会直接走到 `handleBackAtBoundary()` → `finishAndRemoveTask()` → 应用直接退出。

**修复**：改用现代 `OnBackPressedDispatcher` + `OnBackPressedCallback`：
- 有历史 → `webView.goBack()`
- **根页面 → `moveTaskToBack(true)` 退到后台，不杀进程**（解决"直接退出"）

```java
private void setupBackHandling() {
    getOnBackPressedDispatcher().addCallback(this, new OnBackPressedCallback(true) {
        @Override
        public void handleOnBackPressed() {
            if (webView != null && webView.canGoBack()) {
                webView.goBack();
            } else {
                moveTaskToBack(true);  // 根页面退后台而非退出
            }
        }
    });
}
```

同步清理：移除已废弃的 `onBackPressed()`、`handleBackAtBoundary()`、`doubleBackToExit` 字段、`Handler`/`Looper` 导入；`NativeBridge.onBackPressed()` 改为委托 `getOnBackPressedDispatcher().onBackPressed()`。

---

### 修复 C：统一页面顶部色块渐变（解决问题 4）

**文件**：`shared/tokens.css` + `shared/base.css` + `Bot创建向导.html` + `聊天页.html`

**根因**：各页面硬编码不同 header 渐变——
- `base.css .title-bar`：`linear-gradient(135deg, var(--color-primary), var(--color-primary-light))`（粉）
- `Bot创建向导.html .header`：`linear-gradient(135deg, #F472B6 0%, #F9A8D4 50%, #FFB0A0 100%)`（粉→珊瑚，三色）
- `聊天页.html .title-bar`：`linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%)`（token 双色）
- `登录页.html .header`：无背景色（透明，靠 body 渐变）

切换页面时顶部色块跳变 = "不同步"。

**修复**：
1. `tokens.css` 新增统一变量 `--gradient-header: linear-gradient(135deg, #F472B6 0%, #F9A8D4 100%)`
2. `base.css .title-bar` 改用 `var(--gradient-header, ...)`（所有继承页自动统一）
3. `Bot创建向导.html`、`聊天页.html` 的自定义 header 改用同一变量
4. `登录页.html` 保留浅色设计（它本就该与功能页不同色，属合理设计）

**验证**（视觉模型实测）：首页顶部 = **dark pink**，统一生效。

---

### 修复 D：MainActivity 诊断加固 + 白屏兜底（解决问题 5）

**文件**：`agnescord-android/android/app/src/main/java/com/deepseekqq/agnescord/MainActivity.java`

**根因**：① `onReceivedError` 只对 `index.html` 失败才显示兜底，`启动页.html` 失败时静默白屏；② 无 `onReceivedHttpError`（旧回调不覆盖 401/404）；③ 无 WebView 远程调试，白屏时无法定位 JS 错误。

**修复**：
1. `WebView.setWebContentsDebuggingEnabled(true)` —— 开启后可用 `chrome://inspect` + CDP 远程调试
2. `onPageFinished` 打印 URL + `[DIAG]` JS 状态（`API`/`NativeApp`/`APP_CONFIG`/`bodyHas`）
3. 新增 `onReceivedHttpError` —— 捕获 HTTP 错误（401/404 等）
4. `showLoadError()` 对**所有** HTML 页面失败显示兜底（不再只认 `index.html`），且跳过非 HTML 资源错误（避免单个 css/js 404 导致整页空白）
5. `启动页.html` 的 `doNavigate()` 加 try/catch —— JS 异常时显示"启动失败 + 重试"而非白屏

**诊断日志格式**（logcat tag `Agnescord:V` / `WebView:V`）：
```
I/Agnescord: onPageFinished: file:///android_asset/%E9%A6%96%E9%A1%B5%E4%BB%AA%E8%A1%A8%E7%9B%98.html
D/WebView:  [DIAG] url=... API=object NativeApp=object APP_CONFIG=http://129.211.7.67:8766 bodyHas=6
E/Agnescord: WebView HTTP error 401 — http://129.211.7.67:8766/api/v1/dashboard
```

---

### 修复 E：测试直登 + mock token 守卫（解决问题 2 的测试通路）

**文件**：`登录页.html` + `shared/api.js`

**改动**：
1. `登录页.html` 新增「🧪 测试直登」按钮 + `testLogin()` —— 注入 `dev.mock.*` token 直接进首页，**完全不碰后端**（手机连不上 8766 时也能进 App 看 UI）
2. `api.js` 的 `_fetch` / `_upload`：mock token 遇 401 时**不清除 token、不强制登出**，各页面 catch 后降级为空状态，保持测试会话可自由导航

**局限**：mock token 后端不认，所以测试直登**无法创建真实 Bot / 拉真实数据 / 聊天**。要拿真实数据必须注册真实账号。

---

## 三、测试直登 vs 真实账号

| 维度 | 🧪 测试直登（mock token） | 真实账号注册 |
|------|---------------------------|-------------|
| 进首页看 UI | ✅ | ✅ |
| 页面自由导航 | ✅ | ✅ |
| 创建 Bot | ❌（后端 401） | ✅ |
| 聊天（WS） | ❌ | ✅ |
| 拉数据（仪表盘/统计） | ❌（空状态） | ✅ |
| 连不上后端时 | ✅ 仍可看 UI | ❌ |

**注册真实账号**：App 注册页 → 任意 11 位手机号 + 验证码 `1234`（开发期固定）+ 昵称 + 密码（≥8 位）→ 即得真实 JWT（access 15min + refresh 7d）。

---

## 四、本次诊断方法沉淀

### 4.1 logcat 抓 App 日志
```bash
adb -s <serial> logcat -c                          # 清缓冲
adb -s <serial> logcat -v time -s Agnescord:V WebView:V AndroidRuntime:E *:F
```
关键 tag：`Agnescord`（MainActivity 自打）、`WebView`（console.log 转发 + `[DIAG]`）、`AndroidRuntime:E`（Java 崩溃栈）、`*:F`（fatal）。

### 4.2 WebView CDP 远程调试（已开启 `setWebContentsDebuggingEnabled`）
```bash
# 1. 找 socket
adb shell cat /proc/net/unix | grep webview_devtools
# 2. forward
adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>
# 3. 列页面
curl http://127.0.0.1:9222/json
# 4. 用 chrome://inspect 连接，或用 CDP WebSocket (ws://127.0.0.1:9222/devtools/page/<id>) 发 Runtime.evaluate / Page.navigate
```
用途：注入 JS（如 localStorage 写 token）、实时看 Console/Network、导航到指定页面。

### 4.3 截图给本地视觉模型（Ollama）
```bash
# 干净截图（必须用 pull，不能经 PowerShell 重定向否则 PNG 被编码污染）
adb shell screencap -p /sdcard/x.png && adb pull /sdcard/x.png D:\x.png && adb shell rm /sdcard/x.png
# 视觉模型描述（D:\Ollama\Ollama\ollama.exe，模型 llava:13b / moondream:1.8b）
set OLLAMA_MODELS=D:\Ollama
"D:\Ollama\Ollama\ollama.exe" run llava:13b "<prompt>" "D:\x.png"
```
> ⚠️ prompt 含空格/括号时 cmd 引号解析有 bug，改用 PowerShell 变量传 prompt。截图必须 `adb pull`，不可 `exec-out > x.png` 经 PowerShell（UTF-16 编码污染 PNG，导致 "invalid file type: text/plain; charset=utf-16le"）。

---

## 五、待办 / 技术债

| # | 项 | 优先级 |
|:--:|----|:------:|
| T1 | 12 个页面补齐 `native.js` 引用（彻底消除 `NativeApp=undefined`） | 低（config.js 已绕过） |
| T2 | access_token 15min 过期 — 测试时需注意，refresh 7d 可自动续 | 低 |
| T3 | 原生启动屏 `@drawable/splash` 与 HTML 启动页渐变色仍可能有差异（本次未深入） | 低 |
| T4 | HTTP 明文 + `usesCleartextTraffic=true` — 生产需 HTTPS + Caddy 反代（见 deploy/README-api.md） | 中（上线前） |
| T5 | `我的Bot.html` 等 12 页未加载 `native.js` 导致 `<html data-platform="native">` 标记缺失 → `[data-platform="native"]` CSS 安全区适配可能未生效 | ✅ 已通过 config.js 全局 `data-platform` 修复 |

---

## 七、第三轮修复（2026-06-24，用户反馈 32 项问题修复）

### 修复详情（见 [未解决问题清单-2026-06-24.md](未解决问题清单-2026-06-24.md)）

本轮针对用户列出的 32 项问题进行三轮分批修复：

**第一批（已修复并验证 ✅）**：
- A1 聊天历史倒置 → `d.messages.reverse()` 正序渲染
- A4 新建 bot 残留演示消息 → 无条件清空 + 空状态提示
- C1 个人资料保存缺生日 → payload 加 `birthday` + 回填选择器修正
- C3 我的页头像不回显 → `getProfile` 补 `avatar_url` 渲染
- C4 字号切换不生效 → `setFontSizeUI` 补 `setAttribute`
- C9 协议邮箱改 `2938897660@qq.com`
- D2 Bot创建向导返回按钮 → `href` 改 `我的Bot.html`
- D3 输入框 focus 展开 → `textarea:focus{height:110px}`
- E1 胶囊速度 300ms → app.js `SLIDE/COMPRESS` 150+150ms + CSS 同步
- E2/E3 胶囊长方形+包裹 → `getItemBounds` width+8/left-4 (ratio 1.31)
- E4 点击方块闪现 → 全局 `-webkit-tap-highlight-color:transparent`
- E8 通知按钮残影 → 涟漪排除跳转元素
- E10 长按浮出 HTML → CSS `user-select:none` + `setOnLongClickListener`
- A5/A6 聊天页 bot 信息回填 → `getBot` 读 personality/avatar_url

**第二批（已修复 ✅）**：
- A2 图片上传 → 后端 `POST /messages/upload-image` + api.js `uploadChatImage`
- C5 通知开关接 API → `toggleToast` 调 `updateUserSettings`
- C7 返回位置恢复 → `scroll` 事件持续保存 `scrollTop`
- D1 左滑 Bot 卡片 → touch 手势 + 设置/删除操作层
- E5 首页活跃度 → 改显示数字（messages 数）
- B4 用量明细 → `loadUsageDetail` 函数 + `window` 暴露

**第三批（已修复 ✅）**：
- A7 语音 → 原生 MediaRecorder 录音桥接 + WeChat 风格 UI + localStorage 持久化
- C6 系统权限 → Manifest 加 5 权限 + `ActivityCompat.requestPermissions` + `onRequestPermissionsResult`
- config.js 权限桥接移入确保全页面可用
- 聊天背景泄漏修复（app.js `loadChatBgGlobal` 加聊天页判断）
- bg-picker 选色后不自动关闭
- 13 个页面 `.phone` 全屏适配（config.js 全局 `data-platform`）

### 编译状态
全部改动已通过 `gradlew assembleDebug` 编译，APK 已安装到真机。

### 未解决问题
详见独立文档：[未解决问题清单-2026-06-24.md](未解决问题清单-2026-06-24.md)

---

*记录人：ZCode 真机调试会话（2026-06-23 ~ 2026-06-24）*
*设备：OPPO PKG110 Android 16 / SDK 36*

---

## 八、第四～九轮修复（2026-06-24，持续调试）

### 8.1 文件上传：WebView 缺 `onShowFileChooser`（第 7 轮）

**根因**：`MainActivity.java` 的 `WebChromeClient` 只重写了 `onConsoleMessage`，没有 `onShowFileChooser`。WebView 收到 `<input type="file">` 点击后不知道如何处理 → 点上传按钮无反应。

**修复**：
- Java：`WebChromeClient.onShowFileChooser` + `ActivityResultLauncher<OpenDocument>`（AndroidX 文件选择器）
- HTML：`<input type="file">` 从 `display:none` 改为 `opacity:0`（Android WebView 不给隐藏元素弹选择器）

**文件**：`MainActivity.java` + `聊天页.html`

### 8.2 键盘输入框浮现（第 3～9 轮，共 9 次尝试）

**根因**：OPPO ColorOS 对 `adjustResize`/`adjustPan` 均不响应。CSS 层面（`translateY`/`margin-top`/`padding-bottom`）移动 flex 子元素会牵连导航栏；Java 层面 `setLayoutParams` 缩小高度会压缩布局；`setTranslationY` 会整体平移。

**最终方案（第 9 轮）**：
- Java：`ViewTreeObserver.OnGlobalLayoutListener` 检测键盘物理高度 → 除以 `density` 转 CSS px → `evaluateJavascript` 传给 `_onKeyboardChanged(cssPx)`
- JS：`inputBar.style.position = 'fixed'; inputBar.style.bottom = cssPx + 'px'` —— 仅输入栏脱离文档流浮到键盘上方，其余元素不动

**文件**：`MainActivity.java` + `聊天页.html`

### 8.3 个人资料保存失败（第 9 轮）

**根因**：前端 `saveProfile` 传中文性别值 `"男"/"女"/"自定义"`，后端 Pydantic `ProfilePatch.gender` 正则 `^(male|female|custom)$` 不匹配 → 422 → `_parseError` 返回模糊的"请求失败"。

**修复**：
- 前端加 `GENDER_MAP = { '男':'male', '女':'female', '自定义':'custom' }` 映射
- `_parseError` 改进 FastAPI 数组格式 `detail` 的解析（显示具体校验失败字段）

**文件**：`编辑个人资料.html` + `shared/api.js`

### 8.4 Bot 设置保存失败（第 9 轮）

**根因**：聊天页右上角菜单 `<a href="Bot设置.html">` 没有 `?bot_id=` 参数。Bot设置页 `BOT_ID` 从 URL 提取，缺参数 → 保存时 `API.updateBot(undefined, data)` 失败。

**修复**：聊天页初始化后 JS 动态设置 `settingsLink.href = 'Bot设置.html?bot_id=' + botId`。

**文件**：`聊天页.html`

### 8.5 语音功能屏蔽（第 9 轮）

**问题**：录音后播放不稳定 + 切页面回来自动发送语音。暂时隐藏麦克风按钮：
- HTML 麦克风按钮加 `style="display:none"`
- CSS 空输入时纸飞机始终可见（不再切换为麦克风）

### 8.6 输入框展开改为随内容自适应（第 9 轮）

**改动**：删除 `textarea:focus { height: 110px }`，改为 `input` 事件中 `this.style.height = Math.min(this.scrollHeight, 110) + 'px'`。输入内容换行时自动变高，删光缩回。

### 8.7 字体大小修复（第 7 轮）

**根因**：`syncFontSize` 运行时 DOM 可能未就绪；用 `getElementById('phoneFrame')` 部分页面无此 id。

**修复**：`DOMContentLoaded` 事件 + `querySelector('.phone')` 兜底。

**文件**：`shared/app.js` + `设置.html`

### 8.8 聊天页演示消息清除（第 7 轮）

**根因**：静态 HTML 硬编码了完整演示对话（约 70 行），API 加载慢或失败时残留。

**修复**：`<div class="message-list">` 初始内容只留"加载中…"占位。

**文件**：`聊天页.html`

### 8.9 生日选择器（第 5～7 轮）

**根因**：初版 `scroll-snap` 在 Oppo WebView 不可交互；第二版 `addEventListener` 双重绑定；第三版 `scrollIntoView` 在弹窗 `position:fixed` 内不生效。

**当前方案**：tap-to-select + `requestAnimationFrame`×2 延迟计算 `scrollTop`。**用户反馈仍有日期缺失，待进一步调试。**

**文件**：`编辑个人资料.html`

### 8.10 语音播放 base64 支持（第 8 轮）

**根因**：`NativeBridge.playAudio` 用 `contains("/")` 判断 base64，但 base64 data URL 含 `data:audio/amr;base64,...` 中的 `/`，误判为文件路径。

**修复**：改为 `startsWith("data:")` 判断。

**文件**：`MainActivity.java`

### 编译状态

全部改动通过 `gradlew assembleDebug` 编译，APK 已安装到 OPPO PKG110 真机。

### 当前未解决问题

详见：[未解决问题清单-2026-06-24.md](未解决问题清单-2026-06-24.md)

---

## 六、第二轮修复（2026-06-23 晚，4 项任务）

### 6.1 App 聊天"未配置 APIkey"——已解决（服务器端）

**根因**：8766 的 `.api.env`（`deepseek-api` 服务的 EnvironmentFile）**不含 `DEEPSEEK_API_KEY`**——该 key 只在 8082 bot 的 `.env` 里。两个进程独立，env 不共享。8766 进程读 `config.py:66` 得到空值 → `chat.py:54` 直接 yield "未配置"。

**修复**：
1. 服务器 `.api.env` 已补 `DEEPSEEK_API_KEY`/`DEEPSEEK_MODEL`/`DEEPSEEK_BASE_URL`（与 bot 的 `.env` 同值）
2. `sudo systemctl restart deepseek-api` 重启服务，新进程加载含 key 的 env
3. `deploy/api.env.example` 补这 3 项文档（避免下次踩坑）
4. `chat.py:call_deepseek_api_stream` 加降级：`API_KEY` 为空时降级到 `call_deepseek_api`（其内部有 Ollama 兜底）——但**服务器不部署 Ollama**（资源不足），故实际靠 key 配置生效

**验证**：Python websockets 直连 8766 WS → 发"你好，测试一下" → Bot「小咪」流式回复"哼！谁要跟你测试啊……不过既然你问了，那我就勉为其难回应一下吧，你好啦！"（傲娇人设）✅

> ⚠️ 服务器 `chat.py` 仍是旧版（未 git pull 部署新版降级代码），但 key 已配置，旧版也能正常工作。后续如需降级兜底再 pull。

### 6.2 聊天页底部断网横幅 + 模拟键盘去除

**文件**：`聊天页.html` + `shared/dark-mode.css`

**删除内容**：
- `offline-banner`（断网横幅 HTML + CSS + offline/online 监听 JS + `simulateOffline()` + 断网按钮）
- `keyboard-sim`（模拟键盘 HTML + CSS + `toggleKeyboard()` + 键盘按钮）
- `dark-mode.css` 的 `.phone.dark-mode .offline-banner` 残留样式

**原因**：这两个是浏览器原型期的调试组件（模拟断网/模拟键盘弹出），在真机上无意义——真机有自己的网络状态和系统键盘。

### 6.3 导航栏切换速度与胶囊动画对齐

**文件**：`shared/app.js` + `shared/components.css`

**改动**：胶囊动画总时长 **800ms → 470ms**
- `app.js`：`SLIDE_DURATION` 380→220ms，`COMPRESS_DURATION` 420→250ms
- `components.css`：三处 transition 同步——`.nav-capsule` / `.nav-capsule-trail.t1` / `.nav-capsule-trail.t2` 的 `left/top/height` 从 `0.38s`→`0.22s`，`width` 从 `0.42s`→`0.25s`
- 页面跳转（`location.href`）在 470ms 后发生（原 800ms），切换更敏捷，胶囊滑动+压缩动画仍完整

### 6.4 其他功能接入后端（6 个页面）

| 页面 | 问题 | 修复 |
|------|------|------|
| **Bot设置.html** | 3 个按钮 onclick 全是假 toast，保存/清记忆/删除不可用；保存按钮缺 `id="saveBtn"` | 绑定 `saveSettings()`/`clearMemories()`/`deleteCurrentBot()`，加 `id="saveBtn"` |
| **QQ通道.html** | API 用 `[data-msg-count]` 等选择器但 HTML 无此属性 → 三个读取 API 静默失败 | 给状态/QQ号/消息数/群聊数/最近消息元素加 `data-*` 属性；最近消息渲染改用页面一致卡片样式 |
| **管理员面板.html** | `doCreateBackup`/`doLogSnapshot` 未定义 → 点击报 ReferenceError；快捷按钮纯 toast | 定义两函数（调 `API.createBackup()` / `API.getAdminLogs()`），快捷按钮绑定真函数 |
| **黑名单.html** | 4 个写死静态卡片 + `removeUser()` 未定义；无添加拉黑入口 | 删除静态卡片，加"添加拉黑用户"入口（prompt → `API.addToBlacklist()`） |
| **微信通道.html** | `loadWxStatus` 用 `[data-wx-status]` 选择器失效 | 给 `.sc-label` 加 `data-wx-status` 属性 |
| **编辑个人资料.html** | `changeAvatar()` 只轮换猫娘风格，不调 API | 改为文件选择器 → `API.uploadAvatar(file)` 上传真实头像并回显 |

### 6.5 聊天页分辨率 + 顶部对齐（第一轮遗留，本轮验证）

- **聊天页 `.phone` 全屏**：聊天页 `<head>` 漏引用 `base.css`，补加后 `[data-platform="native"] .phone { position:fixed; 360×792 }` 生效（CDP 实测）
- **顶部 padding 统一 44px**：`base.css` native 的 `.title-bar`/`.header` 从 `calc(44px + env(safe-area-inset-top))` 改固定 `44px`；`.top-bar` 改 `48px`；`我的Bot.html` 补 `native.js` 修 `platform=null`
- CDP 实测 4 页 padding-top：mybot/chat/mine = 44px，home = 48px，一致 ✅
