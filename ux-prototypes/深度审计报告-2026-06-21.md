# 安卓控制面板开发计划 — 深度审计报告

**审计日期**：2026-06-21  
**审计方法**：grep 代码库逐项验证 + 文件存在性检查 + 工时交叉校验 + 前后端对应关系审计  
**审计范围**：全部 14 个章节，1966 行文档，55 项任务

---

## 一、已验证通过的声明 ✅ (9 项)

| # | 声明 | 验证结果 |
|---|------|---------|
| 1 | Pipeline 引擎 22 个 stage | ✅ `stages/` 目录 22 个 stage 文件确认 |
| 2 | 现有 1303 tests | ✅ `pytest --collect-only` 确认 |
| 3 | `MY_QQ` 硬编码在 `config.py:81` | ✅ 行 81: `MY_QQ: str = str(getattr(cfg, "my_qq", ""))` |
| 4 | `stage_private_whitelist.py` 丢弃非 MY_QQ 私聊 | ✅ 行 15: `if not ctx.is_group and ctx.user_id != MY_QQ:` |
| 5 | `db_mood.py` 全局单行 `WHERE id=1` | ✅ 确认 `bot_mood` 表硬编码 `WHERE id=1` |
| 6 | session_id 影响面 64 个文件 | ✅ grep 确认 64 文件（非原估 53） |
| 7 | `web_admin.py` 768 行 | ✅ `wc -l` 确认 |
| 8 | CausalContext 是 FIFO 非 LRU | ✅ `causal_context.py:334`: `oldest_key = next(iter(_cc_registry))` |
| 9 | `_MAX_CACHED_SESSIONS = 500` | ✅ 行 302 确认 |
| 10 | UI 原型 22 个 HTML + shared/ (6 CSS + 1 JS) | ✅ 目录验证：22 HTML 文件 + shared/ 含 tokens.css/base.css/components.css/anime-cat.css/dark-mode.css/effects.css + app.js + 7 张 Agnes 人格 PNG |

---

## 二、🔴 严重发现（4 项）

### 2.1 WAL 模式已实现，Task 0.7 是浪费工时

**实际代码**（`db_core.py:36-38`）：
```python
await _db.execute("PRAGMA journal_mode=WAL")
await _db.execute("PRAGMA synchronous=NORMAL")
await _db.execute("PRAGMA busy_timeout=5000")
```

三项配置与计划 7.3 节完全一致，早已在生产环境运行。

**修复**：Task 0.7 改为"验证 WAL 已生效"，0.5d→0.1d。

### 2.2 CausalContext 修复被遗漏

文档 4 处提到此问题（4.3、11、13.2.2、13.6），但 Phase 0-5 均无对应任务。

**修复**：Phase 0 新增任务：`causal_context.py` 加 `last_access` 字段 + 淘汰最久未访问 + 上限 500→2000（1d）。

### 2.3 现有单用户数据迁移方案缺失

`bot_mood`（`WHERE id=1`）、`chat_memory.db`、记忆、好感度等数据改成多租户后归属哪个 bot_id 未定义。

**修复**：Phase 0 新增任务：编写 14 个 DB 的数据迁移 SQL（1d）+ 迁移后验证。

### 2.4 备份/恢复无灾备演练

Phase 0.0.5 做备份，Phase 4.6 做恢复，中间隔 23 周无演练。

**修复**：Phase 0 新增任务：备份脚本完成后立即跑全量恢复演练（0.5d）。

---

## 三、🟠 高优先级发现（5 项）

### 3.1 后端 Python 代码无 i18n 策略
### 3.2 Phase 1.14 工时被低估（1.5d→2.5d）
### 3.3 敏感词过滤方案过于模糊
### 3.4 JWT Refresh Token 无轮换策略
### 3.5 全局速率限制不区分通道

---

## 四、🟡 中优先级发现（3 项）

### 4.1 消息处理失败静默丢失（无 retry 机制）
### 4.2 API Content-Type 规范缺失
### 4.3 Phase 1/2 并行策略存在协调风险

---

## 五、工时修正汇总

| 调整项 | 原 | 新 | 理由 |
|--------|:---:|:---:|------|
| 0.7 WAL 模式 | 0.5d | 0.1d | 已实现 |
| 0.新增 CausalContext LRU | — | 1d | 漏掉 |
| 0.新增 数据迁移 SQL | — | 1d | 漏掉 |
| 0.新增 备份恢复演练 | — | 0.5d | 漏掉 |
| 1.14 语音/图片 API | 1.5d | 2.5d | 低估 |
| 1.新增 敏感词过滤引擎 | — | 0.5d | 漏掉 |
| 2.新增 Android 权限系统 | — | 0.5d | 漏掉 |
| **总计增加** | | **+3.1d** | |

---

## 六、结论

计划方向正确，架构合理。修正 7 项后总工时增加约 3.1d（+1 周），仍在容差范围内。
