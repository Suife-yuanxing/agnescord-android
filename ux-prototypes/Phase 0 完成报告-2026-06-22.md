# Phase 0 完成报告

> 日期：2026-06-22（2026-06-23 更新：服务器部署完成 + Phase 3-4 收官）
> 状态：**14/14 项完成，1318/1318 全量测试通过，P0 三件套端到端贯通，16/16 功能页 API 全接通，f19d976 已部署服务器**

---

## 一、产出统计

| 类别 | 数量 | 明细 |
|------|:---:|------|
| **新建文件** | 11 | `db_platform.py` `multi_tenant.py` `llm_queue.py` `rate_limit.py` `api_platform/` (7文件) `scripts/backup_dbs.sh` |
| **惰性化改造** | 3 | `__init__.py` `config.py` `db_core.py`（支持 8766 独立进程）|
| **session_id 迁移** | 30 | `generate_session_id()` 替代 `f"private_{x}"` / `f"group_{x}"` |
| **per-bot 化改造** | 2 | `database.py`（ALTER TABLE）+ `db_mood.py`（4函数加 bot_id）|
| **人设/owner 抽象** | 2 | `multi_tenant.py`（get_owner_qq + get_bot_persona）+ `prompt.py` |
| **API 路由层** | 1 | `api.py`（call_deepseek_api 加 api_config 参数）|
| **proactive 更新** | 4 | morning/night/sleep/shared → get_owner_qq() |
| **其他** | 3 | `causal_context.py`（LRU）+ `stage_private_whitelist.py`（拆除）+ `migrations.py`（m18 try/except）|
| **测试** | 1 | `test_api_platform_p0.py`（9 项 P0 集成测试）|

---

## 二、关键架构决策

| 决策 | 内容 |
|------|------|
| **进程隔离** | 8766 API 进程独立于 8082 NoneBot2 进程，通过惰性化 config/db_core/__init__.py 实现 |
| **DB 共享** | 两进程共享 SQLite（WAL 模式），db_core.get_db() 连接池复用 |
| **向后兼容** | 所有 per-bot 改造用默认参数（bot_id=1, api_config=None）保证现有调用者零改动 |
| **P0 WS 方案** | 方案 B（绕过 Pipeline），App 聊天无情绪/记忆增强——完整版需方案 A spike |

---

## 三、延迟项

| 项 | 原因 |
|------|------|
| 0.10 session_id 收尾 | 剩余文件只读取/传递 session_id，不构造，非阻塞 |
| 0.13 迁移脚本 | 需生产数据语境 |
| 0.13a 数据迁移 SQL | 同上 |
| 0.13b 备份恢复演练 | 依赖 0.13 + 生产环境 |

---

## 四、下一步（2026-06-23 更新）

Phase 1 **17/17 Task 全部完成**（2026-06-22 收官，1318/1318 全量测试通过），**前后端 API 全部对接完成**（2026-06-23，16/16 功能页接通）：

| 优先级 | Task | 说明 | 状态 |
|:---:|------|------|:---:|
| P1 | 1.3 API Key KMS | 本地 AES-256-GCM + KMS 升级接口（kms.py + api_keys.py 6端点） | ✅ 2026-06-22 |
| P1 | 1.5 人格模板 API | 6 套预设 | ✅ |
| P1 | 1.7 能力配置 API | LLM/Vision/TTS/STT 开关 | ✅ |
| P2 | 1.8 额度管理 | 日限额 + 队列状态 | ✅ |
| P2 | 1.9 仪表盘聚合 | 首页一键全数据 | ✅ |
| P2 | 1.10 通知 API | 站内通知 | ✅ |
| P2 | 1.11 开发者面板 | lighthouse MCP + 备份/日志 | ✅ |
| P2 | 1.14 语音/图片 API | STT/Vision/TTS HTTP 壳层 | ✅ |
| P2 | 1.15 敏感词过滤 | pyahocorasick | ✅ |
| P3 | 1.16 统计聚合 | 数据面板 8 端点（含话题/成就） | ✅ |
| P3 | 1.17 通道管理 | QQ/微信状态 API | ✅ |
| P3 | 1.12 运维 | systemd 8766 + 防火墙（SSL 留 Phase 5 Caddy） | ✅ 2026-06-22 |
| 🆕 | 前端对接 | 16/16 功能页 API 全接通（3 页修复 + 新增 3 端点，6 纯静态页无需 API） | ✅ 2026-06-23 |

**已部署**：新增的 3 个后端端点（`DELETE /bots/{id}/memory`、`GET /messages/search`、`clear_bot_memory()`）及 Phase 3-4 全部变更已于 2026-06-23 部署到服务器（`f19d976`），`systemctl restart deepseek-api` 完成。

---

## 五、APP 落地（2026-06-22 → 2026-06-23 更新）

内测期方案：**纯原生 WebView 套壳**——27 个 HTML 原型直接用，改 UI 成本低。等内测稳定、要上架应用商店，再按原计划 Flutter 重写。

| 产出 | 说明 |
|------|------|
| `shared/api.js` (813+行) | API 客户端：fetch + JWT 自动刷新(F6) + WebSocket + client_id 幂等 |
| `shared/config.js` (29行) | 服务器地址三级优先级配置 |
| 注册页.html | 接真 `/auth/sms` + `/auth/register`，注册成功跳向导 |
| 登录页.html | 接真 `/auth/sms` + `/auth/login`，已登录自动跳 |
| Bot创建向导.html | 完成按钮调 `POST /bots`，成功跳聊天页 `?bot_id=` |
| 聊天页.html | 接真 WS 流式逐字 + `/messages` 历史 + 断线重连 |
| **2026-06-23 新增** | API Key管理/数据面板/管理员面板 等全部 16 页接通 |
| `server.py` | 加 StaticFiles 托管原型到 8766 根路径（浏览器调试） |

**原生 Android APK 打包**：2026-06-22 晚 Capacitor 初版 → 2026-06-23 迁移为纯原生 WebView（去除 Capacitor/@capacitor/android/capacitor-cordova-android-plugins 依赖），debug APK 12MB，Gradle 自动复制资产。

**APK 稳定性修复（2026-06-23e）**：
| 修复 | 说明 |
|------|------|
| SplashScreen 闪退 | `SplashScreen.installSplashScreen(this)` 必须在 `super.onCreate()` 之前 |
| WebView 双导航冲突 | `shouldOverrideUrlLoading` API 24+ `return false`（原来 `return true` + 自己调 `loadUrl()` 导致与 `location.replace()` 冲突） |
| 登录限制移除 | 8 页移除 `API.isLoggedIn()` 跳转守卫 + `api.js` 401 不再自动跳登录页 + 启动页直接 `location.replace('首页仪表盘.html')` |
| 返回键统一 | `native.js` 精简：移除 history guard/popstate/JS 双击退出 → 全部由 `MainActivity.onBackPressed()` 管理 |
| WebView 容错 | `new WebView()` 包裹 try-catch 回退 TextView + `onReceivedError` 兜底 HTML + `shouldOverrideUrlLoading` 双签名兼容 |
