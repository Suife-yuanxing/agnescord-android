# 林念念 Android App — P0 功能实现方案

## 背景

**App 现状**：纯 WebView 混合 App，全部 UI 由 27 个 HTML 页面（`assets/`）+ `shared/api.js`（约 60 个 API 函数）构成。后端 FastAPI 8766 端口已完整运行，包括 7 个后台定时任务（reminder_check 每 60 秒写入通知）。但 **App 没有系统通知栏推送、没有后台轮询、没有版本更新机制** —— 通知只在用户打开"通知.html"时可见。

**目标**：补齐 P0 核心缺失功能，使 App 具备完整的推送通知和版本管理能力。

---

## 项目信息

| 项目 | 值 |
|------|-----|
| 包名 | `com.deepseekqq.agnescord` |
| 源码目录 | `android/app/src/main/java/com/deepseekqq/agnescord/` |
| compileSdk / targetSdk | 36 |
| minSdk | 24 |
| versionCode / versionName | 1 / "1.0" |
| 构建系统 | Gradle (AGP 8.13.0), Java 17 |
| 后端地址 | `http://129.211.7.67:8766` |
| 版本变量 | `variables.gradle`（集中管理依赖版本） |

---

## 五个 P0 功能概览

| 编号 | 功能 | 说明 |
|------|------|------|
| P0-1 | 通知渠道注册 | 创建 3 个 NotificationChannel（Android 8+ 必需） |
| P0-2 | 后台轮询服务 | ForegroundService 每 75s 查未读数，有新通知弹系统通知栏 |
| P0-3 | 通知点击处理 | 点击通知 → PendingIntent → 跳转对应页面 |
| P0-4 | 版本更新检查 | 启动时对比本地/服务器版本，弹强制/建议更新对话框 |
| P0-5 | JS Bridge 扩展 | NativeBridge 新增 5 个方法供 JS 调用 |

---

## 架构图

```
App (新建 Application 类)
└── registerActivityLifecycleCallbacks(AppForegroundTracker)

MainActivity (修改)
├── onCreate()
│   ├── NotificationChannelHelper.createChannels()              ← P0-1
│   ├── 请求 POST_NOTIFICATIONS 权限（API 33+）                   ← P0-2 前置
│   ├── handleNotificationIntent(getIntent())                   ← P0-3 冷启动
│   ├── NativeBridge 注册 (新增 5 个 @JavascriptInterface)       ← P0-5
│   └── Handler.postDelayed → AppUpdateChecker.check()          ← P0-4 (不依赖页面名)
├── onNewIntent() → handleNotificationIntent()               ← P0-3 热启动
└── onResume()/onPause() → AppForegroundTracker              ← P0-2 辅助

PollingForegroundService (新建 Service)                       ← P0-2
├── ScheduledExecutorService (每 75s)
├── PollingTask → HTTP GET /notifications/unread-count
├── 对比上次 count → 有增量 → NotificationHelper.show()
└── AppForegroundTracker.isForeground() → 前台跳过弹通知

PollingAlarmReceiver (新建 BroadcastReceiver)                 ← P0-2 保活
└── 每 15min → startForegroundService()

BootReceiver (新建 BroadcastReceiver)                         ← P0-2 开机自启
└── 有 Token 时 → PollingScheduler.schedule()
```

---

## 新建文件清单（10 个 Java + 1 个资源 + 1 个网络配置）

### 0. `util/AppUpdateChecker.java`

```java
// 版本更新检查工具类
// - check(Context): 异步 HTTP GET /api/v1/app/version
//   响应格式: { "latest_version": "2.0", "min_version": "2.0", "update_url": "market://...", "force_update": true }
//   对比 versionCode（int 比较），min_version > 当前 → 强制更新对话框（不可关闭）
//   latest_version > 当前但非强制 → 建议更新对话框（可关闭）
//   force_update=true → 强制更新对话框（无关闭按钮，点击更新或退出 App）
// - openAppStore(Context): 打开 update_url 或跳转应用商店
public static void check(Activity activity)
public static void openAppStore(Context context)
```

### 1. `util/NotificationChannelHelper.java`

```java
// 静态工具类，创建 3 个通知渠道（幂等操作）
public static void createChannels(Context context) {
    // CHANNEL_POLLING  — IMPORTANCE_LOW, 无声音, 无角标 (前台服务持久通知)
    // CHANNEL_MESSAGES — IMPORTANCE_DEFAULT, 有声音, 有角标 (新消息/提醒)
    // CHANNEL_SYSTEM   — IMPORTANCE_DEFAULT (版本更新等系统通知)
}
```

### 2. `util/NotificationHelper.java`

```java
// 统一通知构建/发送
public static void showMessageNotification(Context, notifyId, title, body, targetUrl) {
    // notifyId 使用后端消息 ID，确保同一条消息不会覆盖其他未读通知
    // 构建 PendingIntent: intent.setData(Uri) + intent.putExtra("target_url", url)
    // 每次 setAction("NOTIFICATION_" + UUID) 确保 PendingIntent 不复用
    // 使用 CHANNEL_MESSAGES 渠道
}
```

### 3. `util/AppForegroundTracker.java`

```java
// 单例，实现 ActivityLifecycleCallbacks
// 使用 onActivityStarted/onActivityStopped 计数（比 resumedCount 更准确）
// isForeground(): startedCount > 0 + Handler.postDelayed 300ms 去抖
//   防止 Activity 过渡期间（A→B 跳转、旋转、Dialog 主题 Activity）误判为后台
//   导致轮询在 App 前台时错误弹出通知
```

### 4. `util/TokenStore.java`

```java
// JWT access_token/refresh_token 的 SharedPreferences 读写
// 因 WebView localStorage 已是明文，原生端保持同级安全
public static void saveTokens(Context, access, refresh)
public static String getAccessToken(Context)
public static void clearTokens(Context)
```

### 5. `util/PollingScheduler.java`

```java
// AlarmManager 调度封装
public static void schedule(Context)     // 每 15 分钟 setInexactRepeating（非精确闹钟，不需用户授权）
public static void cancel(Context)       // 取消调度
public static void startService(Context) // 启动 PollingForegroundService (兼容 API)
public static void stopService(Context)  // 停止服务
// 注意：用户登出后 Token 清除 → StopService；重新登录 → JS 调用 NativeBridge 通知原生重启
```

### 6. `service/PollingForegroundService.java`

```java
// 前台轮询服务
// - onCreate(): startForeground() + PARTIAL_WAKE_LOCK + ScheduledExecutorService
// - onStartCommand(): START_STICKY
// - onDestroy(): 释放 WakeLock
// - OkHttpClient 配置 10s 连接超时 + 10s 读取超时；5xx 响应做 1 次退避重试（延迟 5s）
// 内部类 PollingTask (implements Runnable):
//   1. 读取 Token (无 Token 跳过)
//   2. HTTP GET /api/v1/notifications/unread-count
//   3. 401 则停止服务
//   4. 从 SharedPreferences 读取上次 unreadCount（键名 polling_last_unread_count）
//   5. 对比当前值，增量 > 0 且 App 在后台 → NotificationHelper.show()
//   6. 将当前 unreadCount 写回 SharedPreferences
//   注意：持久化到 SP 防止系统杀死服务重建后重复弹通知
```

### 7. `receiver/PollingAlarmReceiver.java`

```java
// onReceive() → PollingScheduler.startService()
```

### 8. `receiver/BootReceiver.java`

```java
// onReceive(ACTION_BOOT_COMPLETED) → 有 Token 时 PollingScheduler.schedule()
```

### 9. `res/drawable/ic_notification_small.xml`

通知栏小图标（24dp 白色矢量图）

### 10. `App.java`（新建 Application 类）

```java
// onCreate() 中注册 AppForegroundTracker
registerActivityLifecycleCallbacks(AppForegroundTracker.getInstance());
```

---

## 修改文件清单（5 个）

### 1. `MainActivity.java`

**P0-1 — 通知渠道**:
```java
// onCreate() 中，super.onCreate() 之后：
NotificationChannelHelper.createChannels(this);

// API 33+ 运行时请求 POST_NOTIFICATIONS 权限
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
    if (checkSelfPermission(Manifest.permission.POST_NOTIFICATIONS)
            != PackageManager.PERMISSION_GRANTED) {
        requestPermissions(
            new String[]{Manifest.permission.POST_NOTIFICATIONS},
            REQUEST_CODE_POST_NOTIFICATIONS);
    }
}
```

**P0-3 — 通知点击处理**:
```java
// 新增方法（在类主体中，非 NativeBridge 内部）：
@Override
protected void onNewIntent(Intent intent) {
    super.onNewIntent(intent);
    handleNotificationIntent(intent);
}

private void handleNotificationIntent(Intent intent) {
    String targetUrl = intent.getStringExtra("target_url");
    if (targetUrl == null) {
        Uri data = intent.getData();
        if (data != null) {
            // 使用 Uri 解析而非字符串替换，避免参数解析错误
            targetUrl = data.getQueryParameter("page");
            if (targetUrl == null) targetUrl = data.toString();
        }
    }
    if (targetUrl != null && webView != null) {
        // 使用 JSONArray 做标准 JavaScript 字符串转义，防止注入
        String escapedUrl = JSONArray.wrap(new String[]{targetUrl}).toString();
        escapedUrl = escapedUrl.substring(1, escapedUrl.length() - 1); // 去掉 JSON 数组的 []
        webView.evaluateJavascript(
            "if(window.handleNotification){" +
            "window.handleNotification(" + escapedUrl + ");}", null);
    }
}

// 在 onCreate() 末尾新增：
handleNotificationIntent(getIntent());
```

**P0-4 — 版本更新检查**:
```java
// 在 onCreate() 末尾延迟触发（不依赖特定 HTML 页面名）
// 避免页面改名、deep link 跳转其他页面、网络慢导致 URL 加载失败时跳过检查
new Handler().postDelayed(() -> AppUpdateChecker.check(MainActivity.this), 3000);
```

**P0-5 — JS Bridge 扩展**（在 NativeBridge 内部类中新增 5 个方法）:
```java
@JavascriptInterface
public int getAppVersionCode() {
    try { return getPackageManager().getPackageInfo(getPackageName(), 0).versionCode; }
    catch (Exception e) { return 0; }
}

@JavascriptInterface
public String getAppVersionName() {
    try { return getPackageManager().getPackageInfo(getPackageName(), 0).versionName; }
    catch (Exception e) { return "0.0.0"; }
}

@JavascriptInterface
public void updateApp() {
    runOnUiThread(() -> AppUpdateChecker.openAppStore(MainActivity.this));
}

@JavascriptInterface
public String isForeground() {
    return AppForegroundTracker.getInstance().isForeground() ? "true" : "false";
}

@JavascriptInterface
public void startPollingService() {
    // 用户在 HTML 端重新登录后由 JS 调用，重启后台轮询服务
    PollingScheduler.startService(MainActivity.this);
}
```

### 2. `AndroidManifest.xml`

```xml
<!-- 在 <manifest> 下新增权限 -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<!-- 使用 setInexactRepeating → 不需要 SCHEDULE_EXACT_ALARM（免除用户手动授权）-->

<!-- <application> 添加 android:name=".App" -->

<!-- MainActivity 的 launchMode 改为 singleTop（支持 onNewIntent + 保留 WebView 状态）-->

<!-- <application> 内注册 -->
<service
    android:name=".service.PollingForegroundService"
    android:foregroundServiceType="dataSync"
    android:exported="false" />

<receiver
    android:name=".receiver.PollingAlarmReceiver"
    android:exported="false" />

<receiver
    android:name=".receiver.BootReceiver"
    android:exported="true">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
    </intent-filter>
</receiver>
```

### 3. `app/build.gradle`

```groovy
dependencies {
    // … 已有依赖保留不变 …

    // P0 新增（使用 variables.gradle 中定义的版本）
    implementation "com.squareup.okhttp3:okhttp:$okhttpVersion"
}
```

### 4. `variables.gradle`

```groovy
ext {
    // … 已有变量保留不变 …

    okhttpVersion = '4.12.0'
}
```

### 5. `res/values/strings.xml`

```xml
<string name="channel_polling_name">后台服务</string>
<string name="channel_polling_desc">保持与念念的连接</string>
<string name="channel_messages_name">新消息</string>
<string name="channel_messages_desc">念念发来的消息和提醒</string>
<string name="channel_system_name">系统通知</string>
<string name="channel_system_desc">应用更新等系统信息</string>
```

---

## 关键技术决策

### 为什么用 ForegroundService 而非 WorkManager？
| | ForegroundService + AlarmManager | WorkManager |
|------|------|------|
| 最小轮询间隔 | **无硬性下限（可 60s）** | **硬性下限 15 分钟** |
| Doze 模式 | 持续运行（需前台通知） | 延迟可达 30-60 分钟 |
| 满足需求 | ✅ | ❌ |

WorkManager 的 15 分钟最低间隔完全无法匹配后端 60 秒 reminder_check 频率。前台通知"念念在呢..."对 AI 伴侣 App 友好不突兀。

### 为什么 JWT 不用 EncryptedSharedPreferences？
WebView 端 JWT 已存 `localStorage`（明文），原生端加密无安全增益。后续可统一升级到 Android Keystore。

### launchMode 变更：singleTask → singleTop
- `singleTask` 会清除栈顶 Activity，导致 `onNewIntent` 不触发
- `singleTop` 保留 WebView 状态并正确触发 `onNewIntent`
- 已有 `moveTaskToBack(true)` 处理返回键，不影响单实例行为

---

## Android 版本兼容矩阵

| 功能 | API 24-25 | API 26+ (8.0) | API 31+ (12) | API 33 (13) | API 34+ (14) |
|------|-----------|---------------|--------------|-------------|--------------|
| NotificationChannel | 不需要 | 必须创建 | 必须 | 必须 | 必须 |
| ForegroundService | startService() | startForegroundService() | 同左 | 同左 | 同左 |
| FGS ServiceType | 不需要 | 不需要 | 不需要 | 不需要 | **必须 dataSync** |
| POST_NOTIFICATIONS | 不需要 | 不需要 | 不需要 | **运行时请求** | 同左 |
| PendingIntent FLAG_IMMUTABLE | 不需要 | 不需要 | **必须** | 必须 | 必须 |

---

## 不涉及的内容

- ❌ **不修改** `shared/api.js`（API 函数已完备）
- ❌ **不修改** 任何 HTML 页面
- ❌ **不修改** 后端代码
- ❌ 不实现 FCM/Firebase（后续 P1）
- ❌ 不实现应用角标（后续 P1）
- ❌ 不实现持久 WebSocket（后续 P1）
- ❌ 不实现聊天媒体缓存（后续 P2）

---

## 验证清单

1. **通知渠道**：安装 APK → 系统设置 → 通知 → 林念念Bot，应看到三个渠道
2. **后台轮询**：登录后按 Home 键退出，查看 logcat `PollingTask` 日志
3. **通知弹出**：后端写入新通知，75 秒内收到系统通知栏弹窗
4. **通知点击**：点击通知 → App 打开并跳转 `通知.html`
5. **版本更新**：后端 `/api/v1/app/version` 改 min → 弹强制更新对话框
6. **JS Bridge**：WebView Console 执行 `NativeBridge.getAppVersionName()` → 返回 "1.0"
